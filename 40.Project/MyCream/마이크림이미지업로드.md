

  페이지 연결 구조

  /influencers (page.tsx) - 라우팅 허브
  ├─→ /influencers/onboarding (온보딩 미완료 시)
  │   └─→ 2단계 온보딩 프로세스
  │       1. 약관 동의 (TermsAgreement)
  │       2. KYC 정보 입력 (이름, 이메일, 생년월일)
  │       완료 시 → /influencers/gallery
  │
  └─→ /influencers/gallery (온보딩 완료 시)
      └─→ 갤러리 대시보드
          - 갤러리 탭 (GallerySplitView)
          - 플랜 탭 (PlanGridView)

  플로우 설명

  1. /influencers/page.tsx:4-15: 쿠키 확인 후 리다이렉트
    - influencer_onboarding_complete 쿠키 없음 → /influencers/onboarding
    - 쿠키 있음 → /influencers/gallery
  2. /influencers/onboarding/page.tsx:43: 온보딩 완료 시 갤러리로 이동
    - KYC 완료 → 쿠키 설정 → /influencers/gallery 리다이렉트
  3. /influencers/gallery/page.tsx:7-28: 최종 대시보드
    - 갤러리/플랜 탭 뷰 제공

  이 페이지는 직접 UI를 렌더링하지 않고, 사용자 상태에 따라 적절한 페이지로 자동 리다이렉트하는
  진입점입니다.


이미지 저장: S3 > 업로드 시점에 데이터베이스에 인덱싱

백그라운드 큐로 비동기 인덱싱

 User Upload
      ↓
  Vercel Blob Storage (Object)
      ↓
  Neon Postgres (Metadata + Vector)
      ↓
  CDN Edge Cache 


---
influencerId를 Context에 추가하고 관리

MediaUploadDialog에서 실제 API 호출하도록 수정

GalleryManager에서 DB로부터 미디어 로드

온보딩 완료 시 influencer 레코드 생성

---
- ✅ upsertMedia() - 미디어 삽입/업데이트
  - ✅ upsertSearchIndex() - AI 검색 인덱스 생성

  Read (조회)

  - ✅ getMedia(mediaId) - 단일 미디어 조회
  - ✅ getMediaByUrl() - URL로 미디어 찾기
  - ✅ listMedia(influencerId) - 미디어 목록 (100개)
  - ✅ listMediaWithIndex(influencerId) - 인덱스 포함 목록 (1000개)
  - ✅ getAllMedia() - 전체 미디어
  - ✅ getMediaStats() - 통계
  - ✅ getSearchText() - AI 캡션 조회

  Update

  - ✅ setStatus() - 상태 업데이트
  - ✅ setThumb() - 썸네일 URL 업데이트

  Delete

  - ❌ 개별 미디어 삭제 함수 없음
  - ✅ clearIndexingForInfluencer() - 인덱스만 삭제
  - ✅ clearAllIndexing() - 전체 인덱스 삭제

---
- 갤러리에 이미지 업로드 -> 인덱싱까지 (현재 목업이라 업로드 안됨)
- 플랜 테이블 생성 후 플랜 기능 연동 (등록, 수정, 배포 등)


Plan 테이블 생성
Profile/Character/Voice 테이블 생성


1. 프론트엔드 Context
    - 모든 상태가 `useState`로만 관리
    - API 호출 없음
    - 새로고침 시 데이터 손실
2. 백엔드 DB
    - Media 관련 테이블만 존재
    - Plan/Bucket 테이블 없음
    - Profile 데이터 저장 불가
3. API Layer
    - Media API는 구현됨 (DELETE 제외)
    - Plan API는 임시 구현 (Influencer 조회)
    - Bucket API 전체 없음
4. 연결 부재
    - 프론트엔드 ↔ 백엔드 동기화 없음
    - Context 함수 ≠ API 호출
    - 데이터 일관성 보장 불가



---
1. 이미지 업로드: UI만 있고 실제 파일 업로드 연동 안됨 (GallerySplitView 컴포넌트가 로컬 상태만
  관리)
  2. 플랜 테이블: DB에 plans, plan_media 테이블 없음 (현재 Influencer 테이블을 임시 변환 중)
  3. 플랜 CRUD API: 등록/수정/삭제 엔드포인트 전체 미구현

  구현 계획

  4. 갤러리 업로드 연동: GallerySplitView에서 POST /api/influencers/[id]/media/upload API 호출, 업로드 후 인덱싱까지 자동 진행

  5. 플랜 DB 스키마 생성: plans (id, influencer_id, name, price, status), plan_media (plan_id, media_id) 테이블 생성

  6. 플랜 CRUD API 구현: POST/PATCH/DELETE /api/influencers/[id]/plans, POST /plans/[id]/media 엔드포인트 추가

  7. 플랜 배포 기능: plans.status = 'published' 업데이트 API + 마켓플레이스 노출 로직

  8. 프론트엔드 통합: Context 함수들을 API 호출로 교체, SWR로 자동 동기화 구현



  CREATE TABLE IF NOT EXISTS plans (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    influencer_id UUID NOT NULL REFERENCES influencers(id) ON DELETE CASCADE,
    name TEXT NOT NULL,
    description TEXT,
    price DECIMAL(10,2) NOT NULL DEFAULT 0,
    status TEXT NOT NULL DEFAULT 'draft' CHECK (status IN ('draft','published','archived')),
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
  );


[Plans] <── (1:N) ──> [Plan_Media] <── (N:1) ──> [Influencer_Media] ── (1:1) ──> [Media_Search]

커밋사항 반영


plan, 업로드 기능은 구현했는데 influencer데이터 스키마가 구글, 프로필로 2개로 나눠져있습니다. 그래서 맵핑을 해야하는데 두개를 통합할까요?